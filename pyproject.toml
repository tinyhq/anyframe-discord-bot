[project]
name = "anyframe-discord-bot"
version = "0.1.0"
description = "Discord bot that bug-bashes a repo through an AnyFrame agent session."
requires-python = ">=3.12"
dependencies = [
    "anyframe>=1.0",
    "discord.py>=2.4",
    "pydantic-settings>=2.5",
]
