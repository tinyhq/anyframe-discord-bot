[project]
name = "anyframe-discord-bot"
version = "0.1.0"
description = "Discord bot that bug-bashes a repo through anyframe sandboxes."
requires-python = ">=3.11"
dependencies = [
    "discord.py>=2.4",
    "httpx>=0.27",
]
